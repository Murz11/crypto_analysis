spring:
  profiles:
    active: ci

  datasource:
    url: jdbc:postgresql://localhost:5432/cryptodb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  sql:
    init:
      mode: never

logging:
  level:
    root: INFO
